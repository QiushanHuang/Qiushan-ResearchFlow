# Command: Evolution Resolve

Use this when closing an evolution item.

## Required

```yaml
resolution_status:
resolution_summary:
resolution_evidence:
actions_taken:
not_done:
entered_where:
```

Only use `resolved` when evidence shows the item was actually handled. Otherwise use `partial`, `blocked`, `rejected`, or `superseded`.
