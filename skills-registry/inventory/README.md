# Skill Inventory

Inventory files are snapshots of skills available in a Codex session.

Use the inventory to decide which skill to load for a future task. Treat it as a snapshot: actual availability may change between machines, plugin installs, or Codex sessions.
