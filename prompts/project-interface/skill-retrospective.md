# Prompt: Project Interface Skill Retrospective

Use this prompt after onboarding or after a project milestone.

## Input Contract

- `project_id`:
- `project_path`:
- `researchflow_project_entry`:
- `task_type`:
- `skills_used`:
- `why_selected`:
- `worked_well`:
- `failed_or_costly`:
- `better_next_time`:
- `cross_project_reuse`:

## Output Contract

- global skill usage record;
- project-local skill usage record when an adapter exists;
- candidate skill updates when repeated problems appear;
- evolution inbox entry when the framework needs a change.

Follow `commands/skill-usage-log.md` and `skills/rf-skill-governance/SKILL.md`.
