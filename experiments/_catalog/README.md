# Experiment Catalog

Use this folder for experiment catalog records and simulation folder registries.

Rules:

- Do not recursively scan experiment folders.
- Use registry, manifest, digest, and claim entries.
- Experiment observations do not become domain laws by default.

