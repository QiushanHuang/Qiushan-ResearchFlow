# Resources

Resource notes describe compute, storage, and task-route references for research work across machines and disks.

Indexes are the default entrypoint:

- `indexes/resource.index.jsonl`
- `indexes/storage.index.jsonl`
- `indexes/task-route.index.jsonl`

The first version is recommendation-only. Do not auto-run jobs or move files from these records.
